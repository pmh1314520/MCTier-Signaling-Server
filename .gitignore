# Rust 构建产物
/target/
**/*.rs.bk
*.pdb

# 注意：本项目是可执行程序（binary crate），Cargo.lock 必须提交，
# 以保证 Docker 构建与本地构建依赖版本一致。

# 环境变量与密钥
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
*.pfx

# 反向代理与证书由部署者自行处理，仓库不含相关运行时数据；
# 这里仍然忽略常见的证书目录，防止有人把私钥放进仓库。
certbot/
letsencrypt/
letsencrypt/

# 日志
*.log
logs/

# 编辑器与系统文件
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# 用户投稿共享节点的运行时存档（由服务器生成，不入库）
community_nodes.json
community_nodes.json.tmp

# 临时文件
*.tmp
*.bak
